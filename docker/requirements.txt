# Dependencias Python adicionales para el MVP judicial blockchain
# Se instalan encima de la imagen base odoo:19.0

# Integración blockchain EVM (Ethereum, Polygon)
web3==6.20.1
eth-account==0.11.3

# Cliente Prometheus para exponer métricas desde Odoo (opcional, para fase de métricas)
prometheus-client==0.21.0
